<div class="well">
    <h4>Категории</h4>
    <div class="row">
        <div class="col-lg-6">
            <ul class="list-unstyled">

                {% for category in categories %}
                    <li><a href="{{ path('view_cat', {'id': category.id }) }}">{{ category.name }}</a>
                    </li>
                {% endfor %}
            </ul>
        </div>
        <!-- /.col-lg-6 -->
    </div>
    <!-- /.row -->
</div>